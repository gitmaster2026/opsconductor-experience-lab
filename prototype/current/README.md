# Current Prototype

This folder will hold the active Experience Lab prototype.

Next target: build V4 as a standalone current prototype using the shared data files in `src/data/`.

Do not treat old generated HTML artifacts as production direction. They were useful discovery sketches. V4 should start from the corrected architecture:

- Universe and Risk Board render in the main workspace.
- Dashboard and Passport render in the left context panel.
- Jarvis remains persistent on the right.
- Zoom and Time are separate controls.
- Time affects every lens and panel.
